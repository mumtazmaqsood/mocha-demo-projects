
const {expect, assert} = require('chai');
const should = require('chai').should;

describe('Chai Asseration', () => {
    const a = 20;
    arr = [10, 20, 30];
    myFun = (a, b) => a + b;
    isTrue = true;
    nan = NaN;
    myUndefined = undefined;
})

it('Assert Style', () => {
    assert.ok(true) 
    assert.typeOf(arr, 'array')
    
});
